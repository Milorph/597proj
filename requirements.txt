# Two-stage IoT IDS -- Python dependencies
# Pinned to lower bounds known to work; newer compatible versions are fine.
numpy>=1.26
pandas>=2.1
scikit-learn>=1.3
scipy>=1.11
matplotlib>=3.7
seaborn>=0.12
xgboost>=2.0
